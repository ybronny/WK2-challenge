
function numArray(start, end) {
    const numbers = [ ];
    for (let i = start; i <=end; i++) {
     numbers.push( i );s
    }
    return numbers;
}

const bNum = 1
const eNum = 13

const numdArray = numArray(bNum, eNum);
console.log(numdArray) 